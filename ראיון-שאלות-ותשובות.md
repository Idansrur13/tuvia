# דף שינון לראיון: שאלות + תשובות קצרות

## יסודות JavaScript

**1. מה ההבדל בין `==` ל-`===`?**
`==` משווה ערכים עם המרת טיפוסים (`"5" == 5` נכון). `===` משווה גם טיפוס (`"5" === 5` לא נכון).
לזכור: תמיד להשתמש ב-`===`.

**2. מה ההבדל בין var, let ו-const?**
`var` - ישן, function scope, אפשר להכריז פעמיים. `let` - block scope, אפשר לשנות ערך. `const` - block scope, אי אפשר לשנות הפניה.
לזכור: ברירת מחדל `const`, אם צריך לשנות אז `let`, אף פעם לא `var`.

**3. מה זה Promise ומה זה async/await?**
Promise = אובייקט שמייצג פעולה אסינכרונית שתסתיים בהצלחה (resolve) או בכישלון (reject).
async/await = דרך נקייה לכתוב Promises שנראית כמו קוד רגיל. `await` מחכה לתוצאה, `try/catch` תופס שגיאות.

**4. מה זה Event Loop?**
JavaScript רץ על thread אחד. ה-Event Loop בודק כל הזמן: אם ה-Call Stack ריק, הוא מכניס משימות מהתור (callbacks, promises).
לזכור: קוד סינכרוני רץ קודם, promises אחר כך (microtask), ואז setTimeout (macrotask).

**5. מה ההבדל בין Authentication ל-Authorization?**
Authentication = "מי אתה?" - זיהוי המשתמש (סיסמה, SMS).
Authorization = "מה מותר לך?" - בדיקת הרשאות אחרי הזיהוי.
לזכור: קודם מזהים, אחר כך מרשים.

**6. מה זה JWT?**
JSON Web Token - טוקן חתום שהשרת נותן אחרי התחברות. הלקוח שולח אותו בכל בקשה, והשרת מאמת את החתימה בלי לשמור session.
מבנה: Header + Payload + Signature.

**7. מה ההבדל בין Cookies, localStorage ו-sessionStorage?**
Cookies - קטן (4KB), נשלח לשרת בכל בקשה, טוב לאימות עם HttpOnly.
localStorage - גדול (5-10MB), נשאר לתמיד, לא נשלח לשרת.
sessionStorage - כמו localStorage אבל נמחק בסגירת הטאב.
לזכור לראיון: לא שומרים טוקנים ב-localStorage כי חשוף ל-XSS. עדיף Cookie עם HttpOnly.

## רשת ו-HTTP

**8. מה ההבדל בין GET ל-POST?**
GET - מבקש מידע, הפרמטרים ב-URL, אפשר לשמור ב-cache.
POST - שולח מידע ליצירה, הנתונים ב-body, לא נשמר ב-cache.

**9. מהם קודי סטטוס חשובים של HTTP?**
200 הצלחה, 201 נוצר, 301 הפניה קבועה, 400 בקשה שגויה, 401 לא מזוהה, 403 אין הרשאה, 404 לא נמצא, 500 שגיאת שרת.
לזכור: 4xx = אשמת הלקוח, 5xx = אשמת השרת. 401 = מי אתה בכלל, 403 = אני יודע מי אתה אבל אסור לך.

**10. מה זה REST API?**
סגנון ארכיטקטורה לבניית API מעל HTTP: משאבים ב-URL (`/users/5`), פעולות דרך המתודות (GET קריאה, POST יצירה, PUT עדכון, DELETE מחיקה), ללא שמירת מצב בשרת (stateless).

**11. מה זה CORS?**
מנגנון אבטחה של הדפדפן שחוסם בקשות מדומיין אחר. השרת צריך להחזיר header בשם `Access-Control-Allow-Origin` כדי לאשר.
לזכור: זו הגנה של הדפדפן, לא של השרת.

## אבטחה

**12. מה זה XSS ואיך מתגוננים?**
Cross-Site Scripting - תוקף מזריק JavaScript זדוני לאתר (למשל בתגובה).
הגנה: escape לכל קלט משתמש, HttpOnly cookies, React מגן אוטומטית כשמשתמשים ב-JSX רגיל.

**13. מה זה SQL Injection ואיך מתגוננים?**
תוקף מכניס קוד SQL בשדה קלט (`' OR 1=1 --`).
הגנה: Prepared Statements / פרמטרים, או ORM כמו Prisma שעושה את זה אוטומטית.

## React ופרונטאנד

**14. מה ההבדל בין State ל-Props?**
Props - מידע שעובר מהאב לילד, לקריאה בלבד.
State - מידע פנימי של הקומפוננטה שמשתנה, וכל שינוי גורם לרינדור מחדש.
לזכור: Props יורדים למטה, State חי בפנים.

**15. מה זה Virtual DOM?**
עותק קל של ה-DOM בזיכרון. React משווה בין הגרסה הישנה לחדשה (diffing) ומעדכן בדפדפן רק מה שהשתנה. זה מה שהופך את React למהיר.

**16. מהם ה-Hooks המרכזיים?**
`useState` - ניהול state. `useEffect` - פעולות צד (fetch, טיימרים) אחרי רינדור. `useContext` - מידע גלובלי בלי להעביר props. `useMemo`/`useCallback` - מניעת חישובים מיותרים.

## בסיסי נתונים

**17. מה ההבדל בין SQL ל-NoSQL?**
SQL (PostgreSQL, MySQL) - טבלאות עם סכמה קבועה, קשרים בין טבלאות, טרנזקציות. טוב למידע מובנה כמו עסקאות ומשתמשים.
NoSQL (MongoDB) - מסמכים גמישים בלי סכמה קבועה. טוב למידע משתנה ובקנה מידה גדול.

**18. מה זה Index בבסיס נתונים?**
מבנה עזר שמאיץ חיפושים, כמו אינדקס בספר. חיפוש הופך מסריקת כל הטבלה למציאה מהירה.
המחיר: כתיבה קצת יותר איטית ותפיסת מקום.

## שאלות אישיות

**19. ספר על עצמך / על פרויקט**
לספר על פלטפורמת הנדל"ן: React Router, Prisma, Supabase, PostgreSQL. ניהול לידים, עסקאות, יחידות וצ'אט. לתאר בעיה אמיתית שפתרת בפרויקט.

**20. מה החולשה שלך?**
לתת חולשה אמיתית אבל לא קריטית + מה אתה עושה כדי להשתפר. למשל: "לפעמים אני צולל עמוק מדי לפרטים, אז למדתי להגדיר לעצמי זמן מוגבל למשימה."
